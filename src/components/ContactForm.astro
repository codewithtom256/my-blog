<form name="contact" method="POST" data-netlify="true">
  <fieldset
    class="fieldset bg-base-200 border-base-300 rounded-box w-full border p-4 mt-8 flex flex-col md:flex-row"
  >
    <div class="w-full md:w-1/3 space-y-2">
      <div class="flex flex-col">
        <label class="label font-semibold text-xs">Name</label>
        <input
          type="text"
          name="name"
          class="input input-md md:input-sm w-full focus:outline-none focus:border-primary"
          placeholder="John Doe"
        />
      </div>
      <div class="flex flex-col">
        <label class="label font-semibold text-xs">Email</label>
        <input
          type="text"
          name="email"
          class="input input-md md:input-sm w-full focus:outline-none focus:border-primary"
          placeholder="John@doe.com"
        />
      </div>
      <button
        type="submit"
        class="hidden md:flex btn btn-sm btn-primary mt-4 w-full"
      >
        Send 🚀
      </button>
    </div>
    <div class="flex-1 w-full">
      <div class="flex flex-col h-full">
        <label class="label font-semibold text-xs">Message</label>
        <textarea
          name="message"
          class="textarea w-full focus:outline-none focus:border-primary textarea-md md:textarea-sm flex-1"
          placeholder="Hi,I would love to collaborate on a new web app project!"
        ></textarea>
      </div>
      <button
        type="submit"
        class="flex md:hidden btn btn-sm btn-primary mt-4 w-full"
      >
        Send 🚀
      </button>
    </div>
  </fieldset>
</form>
